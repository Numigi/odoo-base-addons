<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="staging_environment_form" model="ir.ui.view">
        <field name="name">Staging Environment Form</field>
        <field name="model">staging.environment</field>
        <field name="arch" type="xml">
            <form string="Staging Environment">
                <header>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object"
                                class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_button"
                                options='{"terminology": "archive"}'/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>
                    <group>
                        <group name="group_1">
                            <field name="url" required="1"/>
                            <field name="client" required="1"/>
                            <field name="level" required="1"/>
                            <field name="token" required="1" password="True"/>
                        </group>
                        <group name="group_2">
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="activity_ids" widget="mail_activity"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <record id="staging_environment_list" model="ir.ui.view">
        <field name="name">Staging Environment List</field>
        <field name="model">staging.environment</field>
        <field name="arch" type="xml">
            <tree string="Staging Environments">
                <field name="name"/>
                <field name="url"/>
                <field name="client"/>
                <field name="level"/>
            </tree>
        </field>
    </record>

    <record id="staging_environment_search" model="ir.ui.view">
        <field name="name">Staging Environment Search</field>
        <field name="model">staging.environment</field>
        <field name="arch" type="xml">
            <search string="Search Staging Environments">
                <field name="name"/>
                <field name="url"/>
                <separator/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
           </search>
        </field>
    </record>

    <record id="action_staging_environment" model="ir.actions.act_window">
        <field name="name">Staging Environments</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">staging.environment</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="staging_environment_list"/>
        <field name="search_view_id" ref="staging_environment_search"/>
    </record>

</odoo>
